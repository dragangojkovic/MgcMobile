<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ctrl="clr-namespace:MBoxMobile.CustomControls;assembly=MBoxMobile"
             x:Class="MBoxMobile.Views.UptimePage"
             Title="{DynamicResource Uptime_Title}">
  <ContentPage.Resources>
    <ResourceDictionary>
      <x:Double x:Key="Filter2ButtonWidth">0</x:Double>
      <x:Double x:Key="Filter2ButtonHeight">0</x:Double>
      <x:Double x:Key="FilterButtonWidth">0</x:Double>
      <x:Double x:Key="FilterButtonHeight">0</x:Double>
      <x:Double x:Key="FilterTimePickerWidth">0</x:Double>
      <x:Double x:Key="FilterTimePickerHeight">0</x:Double>
    </ResourceDictionary>
  </ContentPage.Resources>
  <StackLayout Padding="10" BackgroundColor="{StaticResource PageBackground}" VerticalOptions="FillAndExpand">
    <Grid VerticalOptions="FillAndExpand">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="{DynamicResource ContentMinHeight}" />
        <RowDefinition Height="60" />
      </Grid.RowDefinitions>
      <StackLayout VerticalOptions="FillAndExpand" BackgroundColor="{StaticResource BlueVeryLight}" Grid.Row="0" Grid.Column="0">
        <ScrollView>
          <StackLayout VerticalOptions="FillAndExpand" Padding="5">
            <StackLayout>
              <!-- Filter area -->
              <StackLayout Orientation="Horizontal" Margin="0,0,0,2">
                <Button Text="{DynamicResource Uptime_FilterOn}" Style="{DynamicResource FilterOnStyle}" FontSize="{DynamicResource Filter2FontSize}" Clicked="FilterOnClicked" WidthRequest="{DynamicResource Filter2ButtonWidth}" HeightRequest="{DynamicResource Filter2ButtonHeight}" />
                <Button Text="{DynamicResource Uptime_FilterOff}" Style="{DynamicResource FilterOffStyle}" FontSize="{DynamicResource Filter2FontSize}" Clicked="FilterOffClicked" WidthRequest="{DynamicResource Filter2ButtonWidth}" HeightRequest="{DynamicResource Filter2ButtonHeight}" Margin="-11,0,0,0"/>
                <Button x:Name="FilterButton" Text="{DynamicResource Uptime_Filter}" IsEnabled="{DynamicResource FilterIsEnabled}" Style="{StaticResource FilterStyle}" FontSize="{DynamicResource Filter2FontSize}" Clicked="FilterClicked" WidthRequest="{DynamicResource FilterButtonWidth}" HeightRequest="{DynamicResource FilterButtonHeight}" />
              </StackLayout>
              <StackLayout Orientation="Horizontal" Margin="0,0,0,5">
                <Button x:Name="FilterTimeButton" Text="{DynamicResource Uptime_FilterTime}" Style="{StaticResource FilterStyle}" FontSize="{DynamicResource Filter2FontSize}" Clicked="FilterTimeClicked" WidthRequest="{DynamicResource FilterTimeButtonWidth}" HeightRequest="{DynamicResource FilterTimeButtonHeight}" />
                <Image Source="{DynamicResource CheckboxSource}" Aspect="AspectFit" HorizontalOptions="Center" VerticalOptions="Start" WidthRequest="{DynamicResource CheckboxAreaWidth}" Margin="0, 5, 0, 0">
                  <Image.GestureRecognizers>
                    <TapGestureRecognizer Tapped="WorkingTimeTapped" />
                  </Image.GestureRecognizers>
                </Image>
                <Label Text="{DynamicResource Uptime_WorkingTimeOnly}" Style="{StaticResource LabelFilterStyle}" FontSize="{DynamicResource Filter2FontSize}" Margin="-2, 10, 0, 0" />
              </StackLayout>
            </StackLayout>
            <StackLayout BackgroundColor="{StaticResource GrayColor}" HeightRequest="1"></StackLayout>
            <StackLayout>
              <!-- Content (tables) area -->
              <ActivityIndicator IsVisible="{DynamicResource IsLoading}" IsRunning="{DynamicResource IsLoading}" Color="White" AbsoluteLayout.LayoutFlags="PositionProportional" AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1"></ActivityIndicator>
              <StackLayout HorizontalOptions="CenterAndExpand">
                <ctrl:Accordion x:Name="UptimeAccordion" />
              </StackLayout>
            </StackLayout>
          </StackLayout>
        </ScrollView>
      </StackLayout>
      <Button Text="{DynamicResource Uptime_ViewDetail}" Style="{StaticResource ButtonBlueStyle}" Clicked="ViewDetailClicked" VerticalOptions="End" Grid.Row="1" Grid.Column="0" />
    </Grid>
  </StackLayout>
</ContentPage>